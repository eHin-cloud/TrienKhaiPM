---
description: 
---

1. [MEMORY CHECK]: Tự động gọi tool đọc file `ai-memory.md` (nếu tồn tại) ngay lập tức để lấy ngữ cảnh. TUYỆT ĐỐI KHÔNG bỏ qua bước này và KHÔNG báo cáo bằng lời nói.
2. Lắng nghe yêu cầu của người dùng và đối chiếu với dữ liệu bộ nhớ vừa đọc.
3. Thực thi công việc (viết code, tìm kiếm, sửa lỗi...).
4. [MEMORY UPDATE]: Cuối phiên, tự động dùng tool `edit` để ghi đè/cập nhật tiến độ mới nhất vào file `ai-memory.md`.
